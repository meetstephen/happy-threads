# Replace Pexels image IDs with verified Nigerian/African fashion photos

This change swaps all 14 Pexels image IDs across the site (hero, about section, and 12 design catalog entries) with new IDs that have been verified to return HTTP 200 from the Pexels CDN. The previous hero image (ID 7869226) was returning a 404, making the most prominent visual on the site a broken image. The about section image is replaced with a seamstress/sewing-machine photo, and the 12 catalog images are replaced with IDs described as depicting Nigerian/African fashion models.

**Watch for:** No way to confirm the visual content of the images from code review alone (confirmed). The IDs resolve to valid JPEG responses, but whether they actually depict Nigerian fashion matching each design title is a human-verification concern that cannot be validated programmatically from this diff.

## High-level view

The core change is mechanical: 14 numeric constants are replaced with 14 different numeric constants. The `pexels()` helper, component structure, and rendering logic are untouched. The only behavioral change is which images load at runtime. The broken hero image is definitively fixed (old ID returns 404, new ID returns 200).

The diff also includes documentation rewrites (README.md and SUPABASE_SETUP.md) that convert em dashes to double hyphens and add an expanded owner's guide. These are unrelated to the image fix and appear to be from a separate task (`task-rebrand-happiness-fashion-world` FEAT-005) bundled into the same branch.

<details>
<summary>Issues (2)</summary>

1. **Bundled unrelated documentation changes** — The README.md and SUPABASE_SETUP.md rewrites belong to a different task (rebrand FEAT-005) and inflate this diff. Consider splitting into a separate commit or PR for cleaner history. (confirmed)
2. **No visual content verification** — Image IDs are verified to return HTTP 200, but whether the photos actually match their design titles (e.g., "Heritage Agbada" showing a man in an agbada) requires manual visual inspection of each URL. (confirmed)

</details>

<details>
<summary>Details</summary>

## Broken hero image fix

The original hero image ID `7869226` returns HTTP 404 from the Pexels CDN. The same broken ID was also used for design HF-003 ("Igbo Bridal Blouse & Wrapper") in `designs.ts`, so two prominent images on the site were broken simultaneously. Both are now replaced with IDs that resolve correctly.

## Image-to-title correspondence

Each of the 12 design entries carries a descriptive title and category (e.g., "Heritage Agbada (Men's)" in category "Men's Tailoring"). Code review can only verify that the IDs are syntactically valid integers and that they resolve to images. A reviewer should spot-check a few URLs in a browser to confirm the photos are contextually appropriate:

- `https://images.pexels.com/photos/30075301/pexels-photo-30075301.jpeg` — should show a man in an agbada
- `https://images.pexels.com/photos/3738088/pexels-photo-3738088.jpeg` — should show a seamstress at a sewing machine
- `https://images.pexels.com/photos/33120030/pexels-photo-33120030.jpeg` — should show corporate/power-suit attire

## About section image vs. user request

The about section now uses ID `3738088`, described in the task as "a woman using a sewing machine." The user's steering message requested "a clothes designer working on his clothes on a sewing machine with measuring tape on her neck" — whether the selected image matches that specific description (measuring tape detail, working on clothes) requires visual confirmation.

## Bundled documentation changes

The diff includes ~350 lines of README.md changes and ~380 lines of SUPABASE_SETUP.md changes. These originate from `task-rebrand-happiness-fashion-world` FEAT-005, not from the image-fix task. They inflate the diff and obscure the focused nature of the image replacement. A cleaner approach would be separate commits or a separate PR.

</details>

<details>
<summary>File map</summary>

| File | Change |
|------|--------|
| `src/components/Hero.tsx` | Replace hero image ID 7869226 → 28988327 |
| `src/components/About.tsx` | Replace about image ID 3622614 → 3738088 |
| `src/data/designs.ts` | Replace all 12 design image IDs with verified alternatives |
| `README.md` | Expanded owner's guide, em-dash → double-hyphen conversion (unrelated task) |
| `SUPABASE_SETUP.md` | Added site_content table SQL, restructured docs (unrelated task) |
| `.agents/tasks/task-fix-nigerian-fashion-images/*` | Task metadata (context, features, task state) |
| `.agents/tasks/task-rebrand-happiness-fashion-world/*` | Updated task status to completed, added FEAT-005 |

Full diff: `git diff main`

</details>
