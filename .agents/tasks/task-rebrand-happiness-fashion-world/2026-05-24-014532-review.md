# Rebrand to Happiness Fashion World — fix pass (v2)

The v1 review surfaced 6 issues. All 6 have been addressed in a single fix commit. The `@keyframes shimmer` now exists and the `.text-shimmer` class is applied to the BookingCTA heading. `.section-divider` is wired into Craftsmanship and `.card-glow` into Services cards. A shared `BRAND_STATS` constant eliminates the duplicated statistics. The BookingCTA link correctly targets `#process`. `DesignCard` handles broken images with an `onError` fallback showing a Shirt icon. Featured designs are reduced from 5 to 3.

**Watch for:** `@keyframes glow-pulse` is defined in `index.css` but never referenced by any component or utility class — orphaned dead code (confirmed).

**Verdict**: APPROVED

## High-level view

The six v1 issues are resolved correctly. CSS utilities that were dead code are now consumed: `.text-shimmer` on BookingCTA's `<h2>`, `.section-divider` in Craftsmanship, `.card-glow` on each Services card. The `@keyframes shimmer` declaration closes the animation gap. One keyframe remains orphaned (`glow-pulse`) — it was likely intended for future use but currently serves nothing.

The shared constant stores display strings (`'200+'`, `'7+'`) with `as const` typing, imported by both Hero and BookingCTA. Hero appends " yrs" while BookingCTA appends " Years" — a deliberate display difference (compact vs. spelled out) rather than drift.

<details>
<summary>Issues (1)</summary>

1. **Orphaned `glow-pulse` keyframe** — `@keyframes glow-pulse` is defined in `index.css` but no class references `animation: glow-pulse`. Remove it or wire it into a utility class. (confirmed)

</details>

<details>
<summary>Details</summary>

## Orphaned `glow-pulse` keyframe

The `.card-glow` utility uses a static `box-shadow` transition on hover — it does not reference the `glow-pulse` animation. The keyframe sits at the bottom of `index.css` doing nothing:

```css
@keyframes glow-pulse {
  0%, 100% { box-shadow: 0 0 8px 1px rgba(176, 122, 76, 0.1); }
  50% { box-shadow: 0 0 20px 4px rgba(176, 122, 76, 0.2); }
}
```

No component or CSS class applies `animation: glow-pulse`. This is leftover from the original batch of utilities and should be removed to avoid dead code accumulation.

</details>

<details>
<summary>Files changed in fix commit</summary>

| File | Fix applied |
|------|-------------|
| `src/index.css` | Added `@keyframes shimmer` declaration |
| `src/utils/constants.ts` | **New** — exports `BRAND_STATS` with `clientsDressed` and `yearsOfCraft` |
| `src/components/Hero.tsx` | Imports `BRAND_STATS`, uses in stats card instead of hard-coded strings |
| `src/components/BookingCTA.tsx` | Imports `BRAND_STATS`, uses in trust indicators; link changed from `#about` to `#process`; heading uses `text-shimmer` class |
| `src/components/Craftsmanship.tsx` | Uses `section-divider` class on the divider element |
| `src/components/Services.tsx` | Applies `card-glow` class to service cards |
| `src/components/DesignCard.tsx` | Adds `imgError` state + `onError` handler showing Shirt icon fallback |
| `src/data/designs.ts` | Reduced `featured: true` from 5 to 3 designs (HF-001, HF-005, HF-008) |

</details>
